name: 'Bitcoin Whitepaper Summary Test'
steps:
  - action: user_writes
    text: |
      Please read the attached document and provide a concise summary.
    attachments:
      - 'platform.md'
  - action: await_response
    assert_all:
      - 'GitOps'

  - action: user_writes
    text: |
      Explain how the mentoring methodology is called there in familiar way.
  - action: await_response
    assert_all:
      - 'someone in at the deep end'
