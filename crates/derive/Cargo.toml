[package]
name = "synapto-derive"
version = "0.1.0-dev.1"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["derive", "parsing", "proc-macro"] }
quote = "1.0"
