name: 'File Summary Sequential Test'
steps:
  - action: user_writes
    text: ''
    attachments:
      - 'bitcoin.md'

  - action: user_says
    transcript: 'What is the oldest source in the bitcoin document and from what year? Answer with a numeral, not in words.'

  - action: await_response
    assert_contains: '1957'
