name: 'RAG Context Filtering Test'
steps:
  - action: user_writes
    attachments:
      - 'platform.md'

  - action: user_writes
    text: 'Who is the main architect of the platform?'

  - action: await_response
    assert_contains: 'Alice'
    timeout_secs: 30
